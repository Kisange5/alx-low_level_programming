#inlcude "main.h"
#include <stdio.h>
#include <ctype.h>
/**
 * _isdigit - a function that checks for a digit (0 through 9
 * @c: is the character to look
 * Returns 1 if c is a digit
 * Returns 0 otherwise
 */
int _isdigit(int c)
{
	char c;
}	
